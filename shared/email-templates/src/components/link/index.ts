import { Link as BaseLink } from '@react-email/link';

import { styled } from '@/utils/styled';

export const linkStyle = {
  color: '#2592ff',
};

export const StyledLink = styled(BaseLink, linkStyle);
