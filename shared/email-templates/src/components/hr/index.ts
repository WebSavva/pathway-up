import { Hr as BaseHr } from '@react-email/hr';

import { styled } from '@/utils/styled';

export const hrStyle ={
  borderColor: '#e6ebf1',
  margin: '20px 0',
};

export const StyledHr = styled(BaseHr, hrStyle);
