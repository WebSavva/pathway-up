import { User } from './user.model';
import { Email } from './email.model';
import { PasswordChangeRequest } from './password-change-request.model';

export const MODELS = [User, Email, PasswordChangeRequest];
